#include <stdio.h>
#include <math.h>
//q) use the library function to calculate thr arae of square with side a
int main() {
    int a =5;

    printf(" the area of this square is %f\n",pow( a,2)); //pow=power
                           // pow function gives data in double
    
    return 0;
}