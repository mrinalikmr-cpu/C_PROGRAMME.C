#include <stdio.h>
int main()
{
    // write table of 5 ( you should save file before run it)

    int product;
    for (int i = 1; i <= 10; i++)

    {
        product = 5 * i;

        printf(" 5 * %d = %d\n", i, product);
    }

    return 0;
}
